# デプロイログ: PR #7

## PRメタ情報
- **番号**: #7
- **タイトル**: 本番反映: 入力導線の修復と既存データで効く分析（フェーズ1・2）
- **URL**: https://github.com/konishi-1990/mentalLog/pull/7
- **マージ方向**: `product ← main`
- **マージコミット**: `0a4edc07f724713c3918013a731898703772eff4`
- **マージ日時**: 2026-09-08 17:23:06 UTC（2026-09-09 02:23 JST）
- **マージ方式**: merge（マージコミット / PR #1・#3・#5 に合わせた）
- **含まれる変更**: PR #6 のマージ分

## 起動ワークフロー
- **名前**: Deploy to Product (VPS)
- **run ID**: 34256767806
- **run URL**: https://github.com/konishi-1990/mentalLog/actions/runs/34256767806
- **トリガー**: pull_request (closed → merged, base = product)
- **最終結論**: ✅ success（所要 50秒 / job 45秒）

## ジョブ/ステップ結果
| ステップ | 結果 |
| --- | --- |
| Set up job | ✓ |
| Deploy over SSH | ✓ |
| Complete job | ✓ |

## 完了確認（ログ抜粋）
```
=== Deploy started: Wed Sep  9 02:23:17 AM JST 2026 (branch=product compose=docker-compose.prod.yml) ===
--- git fetch & reset ---
   b3dbc07..0a4edc0  product    -> origin/product
HEAD is now at 0a4edc0 Merge pull request #7 from konishi-1990/main
--- docker compose up --build ---
 Container mentallog-php-1 Recreated
 Container mentallog-nginx-1 Recreated
 Container mentallog-db-1 Healthy
 Container mentallog-php-1 Started
 Container mentallog-nginx-1 Started
--- artisan migrate ---
   INFO  Running migrations.
  2026_09_09_100001_add_description_to_checklist_categories_table  5.71ms DONE
--- artisan cache refresh ---
   INFO  Configuration cache cleared successfully.
   INFO  Configuration cached successfully.
   INFO  Routes cached successfully.
   INFO  Blade templates cached successfully.
=== Deploy finished: Wed Sep  9 02:23:56 AM JST 2026 ===
```

- php / nginx コンテナを再作成、db は Healthy 確認済み。
- **マイグレーション1件を適用成功**（`checklist_categories.description` の nullable な列追加のみ）。
- config / routes / blade キャッシュを再構築。
- 完了マーカー `=== Deploy finished ===` を確認。

## 稼働確認
```
https://mentallog.cde.jp/       → HTTP 302 → /login   （routes/web.php:13 の仕様どおり）
https://mentallog.cde.jp/login  → HTTP 200
```

## seeder 実行記録（実施済み）

`script/deploy.sh` に seeder は含まれないため、デプロイ後に手動実行した。

```bash
ssh cdesvr 'cd ~/Cde/mentallog/mentalLog && docker compose -f docker-compose.prod.yml exec -T php \
  php artisan db:seed --class=ChecklistCategorySeeder --force'
ssh cdesvr 'cd ~/Cde/mentallog/mentalLog && docker compose -f docker-compose.prod.yml exec -T php \
  php artisan db:seed --class=ChecklistOptionSeeder --force'
```

### 🔴 判明：PR #5 の seeder も未実行だった

実行前の本番 `checklist_categories` は **3件のみ**だった。

| 実行前 | 実行後 |
| --- | --- |
| `thought_habit` / `body_reaction` / `recovery_action` の3件 | ＋ `intake`（4件） |
| `recovery_action.tracks_effect` = **false** | **true** |
| `description` は全件 NULL | 全件セット |
| `recovery_action`「何もできてない」`is_none` = false | **true** |
| `checklist_options` 21件（`intake` なし） | 26件（`intake` 5件を追加） |

`deploy-5-20260823-0031.md` の「未実施の手動作業」がそのまま残っていたことが確認された。

**これは分析レポート `docs/analysis/report-202609.md` §1 の前提を覆す。**

| レポートの記述 | 実際 |
| --- | --- |
| 「`intake` カテゴリの選択が0件。マスタはあるのに」 | **本番に `intake` カテゴリが存在しなかった**ため、フォームに表示されようがなかった |
| 「`effect_score` / `duration_min` が全件NULL。フォームもバリデーションも実装済みだが一度も入力されていない」 | `tracks_effect` = false だったため**計測欄が一度も描画されていなかった**。コードは正しく、未反映だっただけ |

つまり **問題A・Bの真因は「UI/導線の失敗」ではなく「seeder の未実行」**だった。
本 PR のフェーズ1（3段階ラジオ化・条件付き必須化）は自由入力より確実に良いため有効だが、
「0-10 の自由入力だから埋まらなかった」という因果は成立していない。
**3段階ラジオが実際に埋まるかは、今回はじめて検証可能になった。**

### 既存データへの影響なし

| テーブル | 件数（実行前後で不変） |
| --- | --- |
| `logs` | 28 |
| `log_checklist_selections` | 104 |
| `log_check_item_values` | 182 |
| `log_people` | 14 |
| `effect_score` が入っている選択 | 0 |

管理画面から追加されていた `thought_habit` の2項目（seeder に無いもの）も保持された。
両 seeder は `updateOrCreate` のため冪等で、`is_active` を落としていた選択肢も存在しなかった。

## 本番データでの検証結果

### 重なり数 × スコア — レポート §2① と完全一致 ✅

| 同日の○件数 | n | ストレス | メンタル余裕 |
| --- | --- | --- | --- |
| 0件 | 2 | 5.00 | 8.00 |
| 1件 | 5 | 6.20 | 6.40 |
| 2件 | 15 | 7.47 | **4.47** |
| 3件 | 5 | 7.20 | 4.40 |
| 4件 | 1 | 9.00 | 4.00 |

1件→2件で −1.93 の崖が画面上で再現された。集計ロジックの正しさが実データで裏付けられた。

### クセの個数 × スコア — レポートと1日ぶんズレる（実装が正しい）

| 個数 | n（本実装） | n（レポート） |
| --- | --- | --- |
| 0個 | **7** | 6 |
| 1個 | 9 | 9 |
| 2個 | 6 | 6 |
| 3個 | 5 | 5 |
| 4個 | 1 | 1 |

レポートは「特になし」を明示選択した6日のみを0個としていたが、
本実装は `logs` 起点で数えるため**そのカテゴリで何も選ばなかった1日も0個に含める**（計28日）。
レポート側は1日を落としていた。`stressSourceOverlap()` / `thoughtHabitCount()` の
「○が0件の日を落とさない」設計（計画 §5 フェーズ2 / テスト T2-2）が意図どおり効いている。

### 相関表の並び順 ✅

| 順位 | 組み合わせ | r | n |
| --- | --- | --- | --- |
| 1 | ストレス × メンタル余裕 | **-0.706** | **28** |
| 2 | 体力 × メンタル余裕 | -0.058 | 28 |
| 3 | 睡眠時間 × 体力 | -0.430 | 13 |
| 4 | 睡眠時間 × メンタル余裕 | 0.157 | 13 |
| 5 | 睡眠の質 × メンタル余裕 | -0.011 | 9（参考値） |

**従来トップだった n=8 の「持ち越し感 × コントロール可能度 -0.83」は下位に移動**し、
n=28 で唯一信頼できる「ストレス × メンタル余裕」が1行目になった（計画の狙いどおり）。

### 前日→翌日（新規） — レポート §2④ と一致 ✅

| 組み合わせ | r | n |
| --- | --- | --- |
| メンタル余裕 → メンタル余裕 | **+0.465** | 18 |
| ストレス → ストレス | **+0.079** | 18 |
| 体力 → 体力 | +0.384 | 18 |
| ストレス → メンタル余裕 | -0.280 | 18 |
| 持ち越し感 → メンタル余裕 | -0.323 | 7 |

「ストレスは毎日リセット／メンタル余裕は溜まる」が画面で読めるようになった。

### 欠測バイアス（新規） — レポート §2⑨ と一致 ✅

| | n | ストレス | メンタル余裕 |
| --- | --- | --- | --- |
| 翌日も記録した日 | 18 | 6.83 | 5.22 |
| 翌日が欠測だった日 | 9 | **7.56** | **4.78** |

記録率 28/64 = 44%、最長未記録 10日連続（いずれもレポートと一致）。

### 入力ラグ（新規）

当日入力 23件 / 翌日入力 5件 / それ以降 0件（計28）、平均 **16.1時（UTC 基準）**。

⚠️ **`config/app.php` の `timezone` は `UTC` 固定**のため、この 16.1時は UTC。
JST では **翌日 01:06 頃**になり、レポート §3 の「就業時間内に記録している」という
説明とは逆の姿になる。当日/翌日の件数分類は影響を受けない（UTC 日付は JST 日付より
前にしかずれないため、件数は下限として正しい）。画面には `（UTC 基準）` と明記済み。

`app.timezone` を JST にするかは既存28件の `created_at` の読み方が変わるため、
フェーズ4（時刻帯・就寝/起床時刻）とまとめて判断する。

### 画面の実描画確認 ✅

本番コンテナ上で HTTP カーネルを通して描画（`Auth::setUser` / 読み取りのみ）。

| パス | 結果 | 確認できた要素 |
| --- | --- | --- |
| `/analytics` | HTTP 200 / 64,364 bytes | ストレス源の重なり / 頭の中のクセの個数 / 前日からの持ち越し / 入力のタイミング / 参考値バッジ / 摂取したもの |
| `/dashboard` | HTTP 200 / 8,384 bytes | 未記録連続バナーは非表示（最終記録 9/7・直近未記録2日 < 閾値3日 で正しい） |
| `/logs/create` | HTTP 200 / 67,340 bytes | 「かなり効いた」（3段階ラジオ）/「回復行動ではなくこちら」（摂取物への誘導）/ 摂取したもの |

## 動作確認の観点
1. `/logs/create` — チェックリストを触らずに保存できる（従来と同じ手数）
2. `/logs/create` — 回復行動を選ぶと3段階ボタンが出て、**選ばずに保存するとエラーになる**
3. `/logs/create` — 「何もできてない」を選ぶと他の回復行動が外れ、効いた感を要求されない
4. `/logs/create` — 「摂取したもの」に誘導文が出る
5. `/analytics` — 重なり数カードがレポート §2① と一致する ← **検証済み（上記）**
6. `/analytics` — 相関表の1行目が n=28 のストレス×余裕になっている ← **検証済み（上記）**
7. `/analytics` — 記録率カードに欠測バイアスと入力タイミングが出る
8. `/logs/{id}` — 計測値が NULL の既存ログが壊れずに表示される
9. `/dashboard` — 未記録が3日以上続いていればバナーが出る

## 結果

本番 VPS（mentallog.cde.jp）へのデプロイ成功。**フェーズ1・2ともに全面稼働**。

- スキーマ変更1件（`checklist_categories.description`）適用済み
- seeder 実行済み。**PR #5 分（`intake` カテゴリ / `tracks_effect`）も同時に解消**
- 既存データ（ログ28件・選択104件・○×182件・相手タグ14件）に影響なし
- フェーズ2の分析5種すべてがレポートの数値を再現（重なり数・自己相関・欠測バイアス・入力ラグ・相関の並び順）

### 次に観測すべきこと（計画 §9 の判断ポイント）

**`effect_score` の入力率が今回はじめて意味を持つ。** これまでは計測欄自体が
描画されていなかったため、0/28 は「入力されなかった」ではなく「聞いていなかった」だった。

| 観測する数字 | 見方 |
| --- | --- |
| `effect_score` の入力率 | 回復行動を選んだ日のうち何%で入ったか。3段階ラジオが機能しているかの唯一の指標 |
| `intake` の選択率 | カテゴリが初めて表示された。飲食物が回復行動の「その他」から分離されるか |
| 記録率 | 44% から下がっていないか。下がればフェーズ3以降は着手しない |

2〜4週間後にこれらを見てフェーズ3以降のスコープを決める（要判断 C）。
