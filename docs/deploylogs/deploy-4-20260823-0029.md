# デプロイログ: PR #4

## PRメタ情報
- **番号**: #4
- **タイトル**: ログ記録項目を8点拡張し、分析に反映
- **URL**: https://github.com/konishi-1990/mentalLog/pull/4
- **マージ方向**: `main ← feat/extend-log-fields`
- **マージコミット**: `c04c12c7f08224f9264c6f3a31927c1ac5b978ed`
- **マージ日時**: 2026-08-22 15:29:56 UTC（2026-08-23 00:29 JST）
- **マージ方式**: merge（マージコミット）
- **head ブランチ**: マージ後に削除済み
- **含まれるコミット**:
  - `8499e0b` ログ記録項目を8点拡張し、分析に反映（docs/plan-log-extension.md 全6フェーズ）
  - `9ddd753` デプロイログ（PR #1〜#3）を記録として追加

## 起動ワークフロー
このPRは `main` へのマージのためデプロイは発動しない（`product` への PR マージのみが発動条件）。
本番反映は PR #5 で実施した（`deploy-5-20260823-0031.md` 参照）。

## 実装内容
`docs/plan-log-extension.md` の全6フェーズ。

| フェーズ | 内容 |
| --- | --- |
| 1 | `logs` に `sleep_hours` / `sleep_quality` / `carryover` / `controllability` / `day_type` を追加（全 nullable + CHECK 制約）。定数は `app/Support/DayTypes.php` |
| 2 | 飲酒・カフェインを `intake` カテゴリとして回復行動から分離（seeder のみ、アプリコード変更なし） |
| 3 | `log_checklist_selections` に `duration_min` / `effect_score`。出し分けは `checklist_categories.tracks_effect` フラグ |
| 4 | `people` / `log_people` と相手タグ CRUD（`check_items` の構造・Policy・並び替えを踏襲） |
| 5 | `correlations()` / `dayTypeBreakdown()` / `personFrequency()` / `recoveryEffect()`。相関には有効件数 n を必ず併記 |
| 6 | `coverage()` で記録率・最長未記録連続日数を算出しヒートマップ表示（入力項目は増やさない） |

## テスト結果
`207 passed`（456 assertions）。

`tests/Feature/ExampleTest.php` の1件は `routes/web.php:12` の仕様（`/` は302）により本作業以前から失敗している既知の問題。計画書にも記載があり、今回は触っていない。

## 結果
`main` へのマージ成功。
