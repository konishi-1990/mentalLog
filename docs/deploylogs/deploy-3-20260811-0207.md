# デプロイログ: PR #3

## PRメタ情報
- **番号**: #3
- **タイトル**: 本番反映: 一般ユーザのログイン後に管理画面へ遷移して403になる問題を修正
- **URL**: https://github.com/konishi-1990/mentalLog/pull/3
- **マージ方向**: `product ← main`
- **マージコミット**: `0def93decf859deb218763ca618690f45ce34845`
- **マージ日時**: 2026-08-10 17:07:21 UTC（2026-08-11 02:07 JST）
- **マージ方式**: merge（マージコミット / PR #1 に合わせた）
- **含まれる変更**: `551b5ad` 一般ユーザのログイン後に管理画面へ遷移して403になる問題を修正 (#2)

## 起動ワークフロー
- **名前**: Deploy to Product (VPS)
- **run ID**: 31412452569
- **run URL**: https://github.com/konishi-1990/mentalLog/actions/runs/31412452569
- **トリガー**: pull_request (closed → merged, base = product)
- **最終結論**: ✅ success（所要 2分48秒）

## ジョブ/ステップ結果
| ステップ | 結果 |
| --- | --- |
| Set up job | ✓ |
| Deploy over SSH | ✓ |
| Complete job | ✓ |

## 完了確認（ログ抜粋）
```
 Container mentallog-php-1 Recreated
 Container mentallog-nginx-1 Recreated
 Container mentallog-db-1 Healthy
 Container mentallog-php-1 Started
 Container mentallog-nginx-1 Started
--- artisan migrate ---
   INFO  Nothing to migrate.
--- artisan cache refresh ---
   INFO  Configuration cache cleared successfully.
   INFO  Configuration cached successfully.
   INFO  Routes cached successfully.
   INFO  Blade templates cached successfully.
=== Deploy finished: Tue Aug 11 02:10:14 AM JST 2026 ===
✅ Successfully executed commands to all hosts.
```

- php / nginx コンテナを再作成、db は Healthy 確認済み。
- マイグレーションは対象なし（`Nothing to migrate.`）。今回はコード変更のみのため想定どおり。
- config / routes / blade キャッシュを再構築。
- 完了マーカー `=== Deploy finished ===` を確認。

## 結果
本番 VPS（mentallog.cde.jp）へのデプロイ成功。

一般ユーザが未ログイン状態で `/admin/*` にアクセスした後にログインしても
`/admin/users` へ飛ばされず `/dashboard` へ遷移するようになった。
管理者は従来どおり intended URL（例: `/admin/users`）へ復帰する。

### 動作確認の観点
- 一般ユーザ: `/admin/users` を開く → ログイン画面 → ログイン → `/dashboard` に着くこと
- 管理者: 同手順で `/admin/users` に着くこと
- ログイン済み一般ユーザが直接 `/admin/users` を叩いた場合は引き続き 403（意図した挙動）
